export type LangOption = {
  value: string;
  label: string;
  code: string;
  langPath: string;
};

export type LayoutState = {
  height: string;
  width: string;
  lineHeight: string;
  fontSize: string;
};

export type ThemeOption = {
  value: string;
  label: string;
  filePath: string;
};
